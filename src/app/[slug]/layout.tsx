import "@/styles/case-study-webflow.css";
import "@/styles/case-study-overrides.css";

export default function CaseStudyLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
